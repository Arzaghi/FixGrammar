{
  "name": "fixgrammar",
  "version": "1.0.0",
  "private": true,
  "description": "Build tooling for the FixGrammar Chrome extension (signed CRX3 packaging).",
  "scripts": {
    "build": "node scripts/build.mjs",
    "test": "node tests/test_integration.js"
  },
  "devDependencies": {
    "crx3": "^2.0.0"
  },
  "engines": {
    "node": ">=22"
  }
}
